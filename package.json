{
  "name": "tixhawk-landing",
  "private": true,
  "version": "1.0.0",
  "description": "TicketHawk landing page - Last-minute ticket alerts for concerts and events. Note: Brand name is 'TicketHawk' (see BRANDING.md)",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.10"
  }
}


